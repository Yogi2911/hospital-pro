# Copy this file to .env and adjust as needed.

# Default: local SQLite file, zero setup required.
DATABASE_URL=hospitalpg26.postgres.database.azure.com

# PostgreSQL example (matches Task 5 of the Azure guide).
# Pull the password from Key Vault in real deployments instead of hardcoding it here.
# DATABASE_URL=postgresql+psycopg2://postgres:yourpassword@localhost:5432/hospitaldb
